# Reference this view as omni_dbt_saas__opportunities_sv__contacts
# View is defined in the semantic view OPPORTUNITIES_SV in the data warehouse
schema_label: ""
#This description was pulled from dbt.
description: Table of the contacts records at the accounts for the company Data Blobby

schema: omni_dbt_saas
folder: SAAS/opportunities_sv
table_name: CONTACTS

dimensions:
  id:
    # Dimension is defined in the semantic view OPPORTUNITIES_SV in the data warehouse
    sql: '"ID"'
    format: ID
    #This description was pulled from dbt.
    description: Unique identifier for each contact
    primary_key: true

  account_id:
    # Dimension is defined in the semantic view OPPORTUNITIES_SV in the data warehouse
    sql: '"ACCOUNT_ID"'
    format: ID
    #This description was pulled from dbt.
    description: Account identifier for each contact

  first_name:
    # Dimension is defined in the semantic view OPPORTUNITIES_SV in the data warehouse
    sql: '"FIRST_NAME"'
    #This description was pulled from dbt.
    description: The given first name of the contact

  last_name:
    # Dimension is defined in the semantic view OPPORTUNITIES_SV in the data warehouse
    sql: '"LAST_NAME"'
    #This description was pulled from dbt.
    description: The given surname of the contact

  email:
    # Dimension is defined in the semantic view OPPORTUNITIES_SV in the data warehouse
    sql: '"EMAIL"'
    #This description was pulled from dbt.
    description: The contact's company email address
    synonyms: [ email_address, contact_email ]

  phone:
    # Dimension is defined in the semantic view OPPORTUNITIES_SV in the data warehouse
    sql: '"PHONE"'
    #This description was pulled from dbt.
    description: The contact's phone number if one is given
    synonyms: [ phone_number, contact_phone ]

  title:
    # Dimension is defined in the semantic view OPPORTUNITIES_SV in the data warehouse
    sql: '"TITLE"'
    #This description was pulled from dbt.
    description: The contacts role at the account
    synonyms: [ job_title, role, position, contact_title ]

  created_date:
    # Dimension is defined in the semantic view OPPORTUNITIES_SV in the data warehouse
    sql: '"CREATED_DATE"'
    #This description was pulled from dbt.
    description: The date the contact record was created

measures:
  count:
    # Measure is defined in the semantic view OPPORTUNITIES_SV in the data warehouse
    sql: COUNT(*)
    description: Count of contacts
    synonyms: [ contact_count, total_contacts, number_of_contacts ]

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: contacts
  target_schema: demo
  description: Table of the contacts records at the accounts for the company Data Blobby
  config:
    schema: saas
    materialized: table
  code: select * from {{ ref('int_saas__contacts')}}
