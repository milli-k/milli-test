# Reference this view as omni_dbt_saas__opportunities_sv__product
# View is defined in the semantic view OPPORTUNITIES_SV in the data warehouse
schema_label: ""
#This description was pulled from dbt.
description: Product catalog containing all products available for sale

schema: omni_dbt_saas
folder: SAAS/opportunities_sv
table_name: PRODUCT

dimensions:
  id:
    # Dimension is defined in the semantic view OPPORTUNITIES_SV in the data warehouse
    sql: '"ID"'
    format: ID
    #This description was pulled from dbt.
    description: Unique identifier for each product
    primary_key: true

  name:
    # Dimension is defined in the semantic view OPPORTUNITIES_SV in the data warehouse
    sql: '"NAME"'
    #This description was pulled from dbt.
    description: Product name or title
    synonyms: [ product_name, product_title ]

  family:
    # Dimension is defined in the semantic view OPPORTUNITIES_SV in the data warehouse
    sql: '"FAMILY"'
    #This description was pulled from dbt.
    description: Product family or category grouping
    synonyms: [ product_family, product_category, product_group ]

  created_date:
    # Dimension is defined in the semantic view OPPORTUNITIES_SV in the data warehouse
    sql: '"CREATED_DATE"'
    #This description was pulled from dbt.
    description: Date when the product was added to the catalog

  product_code:
    # Dimension is defined in the semantic view OPPORTUNITIES_SV in the data warehouse
    sql: '"PRODUCT_CODE"'
    #This description was pulled from dbt.
    description: Internal product code or SKU
    synonyms: [ sku, product_sku, item_code ]

measures:
  count:
    # Measure is defined in the semantic view OPPORTUNITIES_SV in the data warehouse
    sql: COUNT(*)
    description: Count of products
    synonyms: [ product_count, total_products, number_of_products ]

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: product
  target_schema: demo
  description: Product catalog containing all products available for sale
  config:
    schema: saas
    materialized: table
  code: select * from {{ ref('stg_saas__products') }}
