# Reference this view as omni_dbt_saas__int_saas__opportunity_history
schema_label: ""

schema: omni_dbt_saas
table_name: INT_SAAS__OPPORTUNITY_HISTORY

dimensions:
  id:
    sql: '"ID"'
    format: ID
    primary_key: true

  account_id:
    sql: '"ACCOUNT_ID"'
    format: ID

  name:
    sql: '"NAME"'

  stage_name:
    sql: '"STAGE_NAME"'

  amount:
    sql: '"AMOUNT"'

  arr:
    sql: '"ARR"'

  acv:
    sql: '"ACV"'

  tcv:
    sql: '"TCV"'

  contract_term_months:
    sql: '"CONTRACT_TERM_MONTHS"'

  probability:
    sql: '"PROBABILITY"'

  close_date:
    sql: '"CLOSE_DATE"'

  type:
    sql: '"TYPE"'

  next_steps:
    sql: '"NEXT_STEPS"'

  lead_source:
    sql: '"LEAD_SOURCE"'

  is_won:
    sql: '"IS_WON"'

  forecast_category:
    sql: '"FORECAST_CATEGORY"'

  owner_id:
    sql: '"OWNER_ID"'
    format: ID

  sales_engineer_id:
    sql: '"SALES_ENGINEER_ID"'
    format: ID

  created_date:
    sql: '"CREATED_DATE"'

  contact_id:
    sql: '"CONTACT_ID"'
    format: ID

  last_stage_change_date:
    sql: '"LAST_STAGE_CHANGE_DATE"'

  first_demo_date:
    sql: '"FIRST_DEMO_DATE"'

  trial_start_date:
    sql: '"TRIAL_START_DATE"'

  competitor:
    sql: '"COMPETITOR"'

  loss_reason:
    sql: '"LOSS_REASON"'

  win_reason:
    sql: '"WIN_REASON"'

  risk_score:
    sql: '"RISK_SCORE"'

  risk_flags:
    sql: '"RISK_FLAGS"'

  identified_pain:
    sql: '"IDENTIFIED_PAIN"'

  risk_details:
    sql: '"RISK_DETAILS"'

  ae_notes:
    sql: '"AE_NOTES"'

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: int_saas__opportunity_history
  target_schema: demo
  config:
    schema: saas
    materialized: table
  code: |-
    WITH PARAMS AS (
      SELECT 
        CURRENT_DATE() AS AS_OF_DATE,
        (YEAR(CURRENT_DATE()) - 2025) AS YEAR_OFFSET 
    )

    SELECT
      -- Identifiers
      ID,
      OPPORTUNITY_ID,
      ACCOUNT_ID,
      NAME,
      STAGE_NAME,
      AMOUNT,
      ARR,
      ACV,
      TCV,
      CONTRACT_TERM_MONTHS,
      PROBABILITY,

      -- When this snapshot was captured (Shifted to 2026)
      DATEADD(MONTH, P.YEAR_OFFSET * 12, CREATED_DATE) AS CREATED_DATE,

      -- When the stage actually changed (Shifted)
      DATEADD(MONTH, P.YEAR_OFFSET * 12, LAST_STAGE_CHANGE_DATE) AS LAST_STAGE_CHANGE_DATE,

      -- Close Date Logic: Cap at current date if the shifted date is in the future
      IFF(
        DATEADD(MONTH, P.YEAR_OFFSET * 12, CLOSE_DATE) > P.AS_OF_DATE,
        P.AS_OF_DATE,
        DATEADD(MONTH, P.YEAR_OFFSET * 12, CLOSE_DATE)
      ) AS CLOSE_DATE,

      -- Lifecycle milestones
      DATEADD(MONTH, P.YEAR_OFFSET * 12, FIRST_DEMO_DATE)  AS FIRST_DEMO_DATE,
      DATEADD(MONTH, P.YEAR_OFFSET * 12, TRIAL_START_DATE) AS TRIAL_START_DATE,

      TYPE,
      IS_WON,
      FORECAST_CATEGORY,
      NEXT_STEPS,
      LEAD_SOURCE,
      OWNER_ID,
      SALES_ENGINEER_ID,
      CONTACT_ID,
      COMPETITOR,
      LOSS_REASON,
      WIN_REASON,
      RISK_SCORE,
      RISK_FLAGS

    FROM {{ ref('stg_saas__opportunity_history') }}
    CROSS JOIN PARAMS P
    WHERE 
      -- Snapshot must have been created by today in our 2026 timeline
      DATEADD(MONTH, P.YEAR_OFFSET * 12, CREATED_DATE) <= P.AS_OF_DATE 
      AND (
        -- AND the stage change must have logically happened by today
        LAST_STAGE_CHANGE_DATE IS NULL 
        OR DATEADD(MONTH, P.YEAR_OFFSET * 12, LAST_STAGE_CHANGE_DATE) <= P.AS_OF_DATE
      )
    ORDER BY CREATED_DATE DESC, ID ASC
