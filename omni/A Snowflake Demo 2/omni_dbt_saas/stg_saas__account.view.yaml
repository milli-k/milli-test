# Reference this view as omni_dbt_saas__stg_saas__account
schema_label: ""

schema: omni_dbt_saas
table_name: STG_SAAS__ACCOUNT

dimensions:
  id:
    sql: '"ID"'
    format: ID
    primary_key: true

  name:
    sql: '"NAME"'

  type:
    sql: '"TYPE"'

  billing_street:
    sql: '"BILLING_STREET"'

  billing_state:
    sql: '"BILLING_STATE"'

  billing_city:
    sql: '"BILLING_CITY"'

  billing_zip:
    sql: '"BILLING_ZIP"'

  billing_country:
    sql: '"BILLING_COUNTRY"'

  region:
    sql: '"REGION"'

  industry:
    sql: '"INDUSTRY"'

  annual_revenue:
    sql: '"ANNUAL_REVENUE"'

  number_of_employees:
    sql: '"NUMBER_OF_EMPLOYEES"'

  segment:
    sql: '"SEGMENT"'

  owner_id:
    sql: '"OWNER_ID"'
    format: ID

  created_date:
    sql: '"CREATED_DATE"'

  csm_id:
    sql: '"CSM_ID"'
    format: ID

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: stg_saas__account
  target_schema: demo
  config:
    schema: saas
    materialized: view
  code: |-
    select {{ dbt_utils.star(source('saas', 'account')) }}
    from {{ source('saas', 'account') }}
  referenced_by: [ int_saas__account ]
