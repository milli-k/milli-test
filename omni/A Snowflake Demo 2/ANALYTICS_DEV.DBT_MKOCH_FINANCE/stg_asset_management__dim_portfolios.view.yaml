# Reference this view as analytics_dev_dbt_mkoch_finance__stg_asset_management__dim_portfolios
catalog: ANALYTICS_DEV
schema: DBT_MKOCH_FINANCE
table_name: STG_ASSET_MANAGEMENT__DIM_PORTFOLIOS

dimensions:
  portfolio_id:
    sql: '"PORTFOLIO_ID"'
    format: ID

  portfolio_name:
    sql: '"PORTFOLIO_NAME"'

  portfolio_type:
    sql: '"PORTFOLIO_TYPE"'

  target_return:
    sql: '"TARGET_RETURN"'

  risk_level:
    sql: '"RISK_LEVEL"'

  inception_date:
    sql: '"INCEPTION_DATE"'

measures:
  count:
    aggregate_type: count
