# Reference this view as analytics_dev_dbt_mkoch_fhir__care_plans
catalog: ANALYTICS_DEV
schema: DBT_MKOCH_FHIR
table_name: CARE_PLANS

dimensions:
  patient_id:
    sql: '"PATIENT_ID"'
    format: ID

  service_provider_id:
    sql: '"SERVICE_PROVIDER_ID"'
    format: ID

  condition_id:
    sql: '"CONDITION_ID"'
    format: ID

  status:
    sql: '"STATUS"'

  category:
    sql: '"CATEGORY"'

  start_ts:
    sql: '"START_TS"'

  end_ts:
    sql: '"END_TS"'

  activity:
    sql: '"ACTIVITY"'

  resource_raw:
    sql: '"RESOURCE_RAW"'

measures:
  count:
    aggregate_type: count
