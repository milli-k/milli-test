# Reference this view as saas__stg_saas__contacts
schema: SAAS
table_name: STG_SAAS__CONTACTS

dimensions:
  id:
    sql: '"ID"'
    format: ID
    primary_key: true

  account_id:
    sql: '"ACCOUNT_ID"'
    format: ID

  first_name:
    sql: '"FIRST_NAME"'

  last_name:
    sql: '"LAST_NAME"'

  email:
    sql: '"EMAIL"'

  phone:
    sql: '"PHONE"'

  title:
    sql: '"TITLE"'

  created_date:
    sql: '"CREATED_DATE"'

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: stg_saas__contacts
  target_schema: demo
  config:
    schema: saas
    materialized: view
  code: |-
    select
      {{ dbt_utils.star(source('saas', 'contacts')) }}
    from {{ source('saas', 'contacts') }}
  referenced_by: [ int_saas__contacts ]
