# Reference this view as omni_dbt_finance__stg_asset_management__dim_portfolios
schema_label: ""

schema: omni_dbt_finance
table_name: STG_ASSET_MANAGEMENT__DIM_PORTFOLIOS

dimensions:
  portfolio_id:
    sql: '"PORTFOLIO_ID"'
    format: ID

  portfolio_name:
    sql: '"PORTFOLIO_NAME"'

  portfolio_type:
    sql: '"PORTFOLIO_TYPE"'

  target_return:
    sql: '"TARGET_RETURN"'

  risk_level:
    sql: '"RISK_LEVEL"'

  inception_date:
    sql: '"INCEPTION_DATE"'

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: stg_asset_management__dim_portfolios
  target_schema: demo
  config:
    schema: finance
    materialized: view
  code: |-
    select
      {{ dbt_utils.star( source('asset_management', 'dim_portfolios') ) }}
    from {{ source('asset_management', 'dim_portfolios') }}
  referenced_by: [ portfolios ]
