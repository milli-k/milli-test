# Reference this view as omni_dbt_finance__stg_asset_management__dim_assets
schema_label: ""

schema: omni_dbt_finance
table_name: STG_ASSET_MANAGEMENT__DIM_ASSETS

dimensions:
  asset_id:
    sql: '"ASSET_ID"'
    format: ID

  asset_name:
    sql: '"ASSET_NAME"'

  asset_type:
    sql: '"ASSET_TYPE"'

  asset_class:
    sql: '"ASSET_CLASS"'

  sector:
    sql: '"SECTOR"'

  current_price:
    sql: '"CURRENT_PRICE"'

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: stg_asset_management__dim_assets
  target_schema: demo
  config:
    schema: finance
    materialized: view
  code: |-
    select
      {{ dbt_utils.star( source('asset_management', 'dim_assets') ) }}
    from {{ source('asset_management', 'dim_assets') }}
  referenced_by: [ assets ]
