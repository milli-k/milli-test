# Reference this view as omni_dbt__query_dbt
schema_label: ""

schema: omni_dbt
table_name: QUERY_DBT

dimensions:
  total_sale_price:
    sql: '"TOTAL_SALE_PRICE"'

  calculation:
    sql: '"CALCULATION"'

  order_id_count_distinct:
    sql: '"ORDER_ID_COUNT_DISTINCT"'

measures:
  count:
    aggregate_type: count
