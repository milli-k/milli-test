# Reference this view as analytics_dev_dbt_aaltmark_ecomm__monthly_sales_overview
catalog: ANALYTICS_DEV
schema: DBT_AALTMARK_ECOMM
table_name: MONTHLY_SALES_OVERVIEW

dimensions:
  month:
    sql: '"MONTH"'

  total_sale_price:
    sql: '"TOTAL_SALE_PRICE"'

  mom_change:
    sql: '"MOM_CHANGE"'

measures:
  count:
    aggregate_type: count
