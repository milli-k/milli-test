# Reference this view as omni_dbt_fhir__int_fhir__organizations
schema_label: ""

schema: omni_dbt_fhir
table_name: INT_FHIR__ORGANIZATIONS

dimensions:
  id:
    sql: '"ID"'
    format: ID
    primary_key: true

  name:
    sql: '"NAME"'

  type:
    sql: '"TYPE"'

  profile:
    sql: '"PROFILE"'

  revenue:
    sql: '"REVENUE"'

  resource_raw:
    sql: '"RESOURCE_RAW"'

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: int_fhir__organizations
  target_schema: demo
  config:
    schema: fhir
    materialized: table
  code: |-
    select
      {{ dbt_utils.star(ref('stg_fhir__organizations')) }}
    from {{ ref('stg_fhir__organizations') }}
