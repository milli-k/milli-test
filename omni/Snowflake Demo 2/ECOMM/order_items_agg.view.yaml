# Reference this view as ecomm__order_items_agg
schema: ECOMM
table_name: ORDER_ITEMS_AGG

dimensions:
  order_id:
    sql: '"ORDER_ID"'
    format: ID

  user_id:
    sql: '"USER_ID"'
    format: ID

  total_sale_price:
    sql: '"TOTAL_SALE_PRICE"'

measures:
  count:
    aggregate_type: count
