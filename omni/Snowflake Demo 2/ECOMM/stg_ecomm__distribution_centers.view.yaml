# Reference this view as ecomm__stg_ecomm__distribution_centers
schema: ECOMM
table_name: STG_ECOMM__DISTRIBUTION_CENTERS

dimensions:
  id:
    sql: '"ID"'
    format: ID
    primary_key: true

  name:
    sql: '"NAME"'

  latitude:
    sql: '"LATITUDE"'

  longitude:
    sql: '"LONGITUDE"'

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: stg_ecomm__distribution_centers
  target_schema: demo
  config:
    schema: ecomm
    materialized: view
  code: |-
    select
      {{ dbt_utils.star(source('ecomm', 'distribution_centers')) }}
    from {{ source('ecomm', 'distribution_centers') }}
  referenced_by: [ distribution_centers ]
