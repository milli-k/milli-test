# Reference this view as ecomm__stg_ecomm__order_items
schema: ECOMM
table_name: STG_ECOMM__ORDER_ITEMS

dimensions:
  id:
    sql: '"ID"'
    format: ID
    primary_key: true

  order_id:
    sql: '"ORDER_ID"'
    format: ID

  user_id:
    sql: '"USER_ID"'
    format: ID

  inventory_item_id:
    sql: '"INVENTORY_ITEM_ID"'
    format: ID

  sale_price:
    sql: '"SALE_PRICE"'

  status:
    sql: '"STATUS"'

  created_at:
    sql: '"CREATED_AT"'

  returned_at:
    sql: '"RETURNED_AT"'

  shipped_at:
    sql: '"SHIPPED_AT"'

  delivered_at:
    sql: '"DELIVERED_AT"'

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: stg_ecomm__order_items
  target_schema: demo
  config:
    schema: ecomm
    materialized: view
  code: |-
    select
      {{ dbt_utils.star(source('ecomm', 'order_items')) }}
    from {{ source('ecomm', 'order_items') }}
  referenced_by: [ int_ecomm__order_items ]
