# Reference this view as ecomm__omni__order_items_curated__ecomm__order_items
# View is defined in the semantic view OMNI__ORDER_ITEMS_CURATED in the data warehouse
schema: ECOMM
folder: ECOMM/omni__order_items_curated
table_name: ORDER_ITEMS

dimensions:
  id:
    # Dimension is defined in the semantic view OMNI__ORDER_ITEMS_CURATED in the data warehouse
    sql: '"ID"'
    format: ID
    primary_key: true

  order_id:
    # Dimension is defined in the semantic view OMNI__ORDER_ITEMS_CURATED in the data warehouse
    sql: '"ORDER_ID"'
    format: ID

  user_id:
    # Dimension is defined in the semantic view OMNI__ORDER_ITEMS_CURATED in the data warehouse
    sql: '"USER_ID"'
    format: ID

  inventory_item_id:
    # Dimension is defined in the semantic view OMNI__ORDER_ITEMS_CURATED in the data warehouse
    sql: '"INVENTORY_ITEM_ID"'
    format: ID

  sale_price:
    # Dimension is defined in the semantic view OMNI__ORDER_ITEMS_CURATED in the data warehouse
    sql: '"SALE_PRICE"'

  status:
    # Dimension is defined in the semantic view OMNI__ORDER_ITEMS_CURATED in the data warehouse
    sql: '"STATUS"'

  created_at:
    # Dimension is defined in the semantic view OMNI__ORDER_ITEMS_CURATED in the data warehouse
    sql: '"CREATED_AT"'

  returned_at:
    sql: '"RETURNED_AT"'

  shipped_at:
    sql: '"SHIPPED_AT"'

  delivered_at:
    sql: '"DELIVERED_AT"'

  is_returned:
    sql: '"IS_RETURNED"'

  is_delivered:
    sql: '"IS_DELIVERED"'

  is_shipped:
    sql: '"IS_SHIPPED"'

  margin:
    sql: '"MARGIN"'

  months_since_signup:
    sql: '"MONTHS_SINCE_SIGNUP"'

  time_to_ship:
    sql: '"TIME_TO_SHIP"'

measures:
  count:
    # Measure is defined in the semantic view OMNI__ORDER_ITEMS_CURATED in the data warehouse
    sql: COUNT(*)

  total_sale_price:
    # Measure is defined in the semantic view OMNI__ORDER_ITEMS_CURATED in the data warehouse
    sql: COALESCE(SUM("SALE_PRICE"), 0)
