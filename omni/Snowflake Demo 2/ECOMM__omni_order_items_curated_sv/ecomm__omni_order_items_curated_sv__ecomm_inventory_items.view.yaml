# Reference this view as ecomm__omni_order_items_curated_sv__ecomm_inventory_items
# View is defined in the semantic view omni_order_items_curated_sv in the data warehouse
schema: ECOMM
folder: ECOMM/omni_order_items_curated_sv
table_name: INVENTORY_ITEMS

dimensions:
  id:
    # Dimension is defined in the semantic view omni_order_items_curated_sv in the data warehouse
    sql: '"ID"'
    format: ID
    primary_key: true

  product_id:
    sql: '"PRODUCT_ID"'
    format: ID

  created_at:
    sql: '"CREATED_AT"'

  sold_at:
    sql: '"SOLD_AT"'

  cost:
    # Dimension is defined in the semantic view omni_order_items_curated_sv in the data warehouse
    sql: '"COST"'

  product_category:
    sql: '"PRODUCT_CATEGORY"'

  product_name:
    sql: '"PRODUCT_NAME"'

  product_brand:
    sql: '"PRODUCT_BRAND"'

  product_retail_price:
    sql: '"PRODUCT_RETAIL_PRICE"'

  product_department:
    sql: '"PRODUCT_DEPARTMENT"'

  product_sku:
    sql: '"PRODUCT_SKU"'

  product_distribution_center_id:
    sql: '"PRODUCT_DISTRIBUTION_CENTER_ID"'
    format: ID

measures:
  count:
    aggregate_type: count

  cost_sum:
    # Measure is defined in the semantic view omni_order_items_curated_sv in the data warehouse
    sql: SUM("COST")
